# Stage 1-3 HTTP work. Everything else is Python standard library.
requests>=2.28

# Reads TLS certificates directly so an expired cert reports its exact expiry
# date rather than just "verification failed". Optional: without it, expired
# certificates are still detected from the OpenSSL error string, just without
# the expiry date.
cryptography>=39.0
